import { defineConfig } from "windicss/helpers";

export default defineConfig({
  attributify: true,
  theme: {
    extend: {
      colors: {
        neon: "#4affff",
        cosmic: "#6f00ff",
      },
    },
  },
});
